variables:
  PC_SKIP_CHECK_LICENSES: "on"
  PC_XC_WRT_DEFAULT: "on"
  PC_UNIT_TESTS: "on"

include:
  - remote: 'https://gitlab.com/prpl-foundation/tooling/yml-templates/raw/main/.component-pipeline.yml'
